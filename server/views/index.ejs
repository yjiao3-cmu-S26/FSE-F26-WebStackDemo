<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Student Profile</title>

  <!--
    TODO 1 — Bootstrap

    Task:
    Add Bootstrap and use Bootstrap classes to style the page.

    Syntax / examples:

    Add Bootstrap CSS:
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >

    Common Bootstrap classes:
    class="container"
    class="mt-5" margin-top: spacing level 5
    class="mb-4" margin-bottom: spacing level 4
    class="card p-4" padding: spacing level 4
    class="btn btn-primary"
    class="text-center"
    class="text-success"
    class="text-primary"
    class="text-secondary"
    ...
  -->

</head>

<body>

  <div>
    <h1>Student Profile</h1>

    <div>

      <!--
        TODO 2 — EJS Variables

        Task:
        Replace the hard-coded name and major with
        variables passed from the Express server.

        EJS output syntax:

        &lt;%= variableName %&gt;

        Example:

        &lt;h3&gt;&lt;%= name %&gt;&lt;/h3&gt;
      -->

      <h3>Student 1</h3>
      <p>Software Engineering</p>


      <!--
        TODO 3 — EJS Conditional

        Task:
        Show a "TA" badge only when isTA is true.

        EJS JavaScript syntax:

        &lt;% if (condition) { %&gt;

          HTML goes here

        &lt;% } %&gt;

        Example Bootstrap badge:

        <span class="badge text-bg-success">
          TA
        </span>
      -->


      <hr>

      <h2>jQuery Demo</h2>

      <!--
        TODO 4 — jQuery

        Task:
        1. Add the jQuery CDN near the bottom of this file.
        2. Load /app.js after jQuery.
        3. In app.js, handle the button click and update
           the text below.

        jQuery syntax:

        Select:
        $('#element-id')

        Listen for an event:
        $('#element-id').on('click', function () {
          // code
        });

        Change text:
        $('#element-id').text('New text');

        Add these scripts in this order:

        <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
        <script src="/app.js"></script>
      -->

      <button id="jquery-btn">
        Run jQuery Demo
      </button>

      <p id="jquery-message">
        Waiting for jQuery...
      </p>

      <hr>

      <h2>Fetch Demo</h2>

      <!--
        TODO 5 — Fetch

        Task:
        Click the button to request a message from /api/hello.

        The Fetch implementation belongs in client/api.js.

        Fetch syntax:

        const response = await fetch('/api/hello');

        const data = await response.json();

        Access the returned value with:

        data.message
      -->

      <button id="api-btn">
        Load Message from API
      </button>

      <p id="api-message">
        Waiting for API response...
      </p>

    </div>
  </div>


  <!--
    jQuery Demo:
    Add the jQuery CDN and /app.js here during TODO 4.

    Keep jQuery before app.js because app.js uses $().
  -->


  <!--
    Fetch Demo:
    api.js uses standard browser JavaScript + fetch().
  -->
    <script src="https://cdn.jsdelivr.net/npm/axios@1.4.0/dist/axios.min.js"></script>

</body>
</html>
